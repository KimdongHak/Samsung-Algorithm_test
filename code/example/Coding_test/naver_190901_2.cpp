/*
입력이 N이 주어지면
그에 따른 값 배열을 구하라 - Vector 사용 필수
그리고 N만큼 값의 개수를 만들어라.
그리고 배열안에 값의 합을 0이 되게 만들어라.

ex) N=4 면 output = [-3,-2,2,3]
 N=3이면 output = [-2,0,2]
*/

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
vector<int> solution{

}

int main() {
	int N;
	cin >> N;
	solution(N);
	return 0;
}